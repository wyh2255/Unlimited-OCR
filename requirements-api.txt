# =============================================================================
# Unlimited-OCR API Server — Dependency Manifest
# =============================================================================
# 安装方式（按顺序）:
#   1. uv venv --python 3.12 && source .venv/bin/activate
#   2. uv pip install wheel/sglang-0.0.0.dev11416+g92e8bb79e-py3-none-any.whl
#   3. uv pip install -r requirements-api.txt
#
# 注意：sglang 定制 wheel 已声明 torch、transformers、ninja、flashinfer
#       等核心 ML 依赖（版本锁定在 wheel 内部），pip install 时会自动安装。
#       不要从 PyPI 安装 sglang —— 必须用项目内的定制 wheel。
# =============================================================================

# ---------------------------------------------------------------------------
# Section 1: ML 层 — sglang wheel 未覆盖的项目级依赖
# ---------------------------------------------------------------------------
pymupdf==1.27.2.2         # PDF 渲染（fitz），用于页面计数和 postprocess 重渲染

# ---------------------------------------------------------------------------
# Section 2: LAN 服务层
# ---------------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
rich>=13.7
requests>=2.31
weasyprint>=60             # 文档转换（PDF 引擎），依赖系统库 libpango

# ---------------------------------------------------------------------------
# Section 3: 开发工具（非运行时必需，但推荐安装）
# ---------------------------------------------------------------------------
# ruff           # 代码检查
# black          # 格式化
# isort          # import 排序
# mypy           # 类型检查
