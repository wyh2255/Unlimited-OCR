"""Marker file — `inference/` becomes a package in Phase 3.

Originally named `sglang/`, renamed in Phase 5 because that name shadowed
the actual `sglang` Python package (where `sglang.launch_server` lives)
when the repo root was on sys.path.
"""
